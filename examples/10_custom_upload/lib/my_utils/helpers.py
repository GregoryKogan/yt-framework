"""Helper functions for the custom upload example."""


def greet(name: str) -> str:
    """Return a greeting string.

    Args:
        name: Name to greet

    Returns:
        Greeting string
    """
    return f"Hello, {name}!"
